from the given image, please help to extract the information and output it as csv. for the column, I need the date (Date) and travel end time (End Time) in AM PM format, mode of transportation (Mode) (either bus, train, or bus and train), the alighting (Origin) and descend place (Destination), and the travel price (Price) with the dollar and it cannot be negative. if the title is a journey, ignore the transaction below until the next group and only get the first data.